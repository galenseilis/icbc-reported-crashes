[project]
name = "icbc-reported-crashes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "galenseilis", email = "galen.seilis@seilis.ca" }
]
requires-python = ">=3.14"
dependencies = [
    "tableauhyperapi>=0.0.26225",
]

[project.scripts]
icbc-reported-crashes = "icbc_reported_crashes:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
